#include<iostream>
#include<conio.h>
int main()
{
        float a,b,c;
        std::cout<<"\nEnter the first number    :";
        std::cin>>a;
        std::cout<<"\nEnter the second number      :";
        std::cin>>b;
        c=a*b;
        std::cout<<"\nThe product of "<<a<<" and "<<b<<" is "<<c;
        return 0;
}
